package main

import (
	"fmt"
)

func main() {
	var strings string

	fmt.Println("fale um nome:")
	fmt.Scan(&strings)
	reverso := ""
	for i := len(strings) - 1; i >= 0; i-- {
		reverso += string(strings[i])
	}
	fmt.Print(reverso)

}
